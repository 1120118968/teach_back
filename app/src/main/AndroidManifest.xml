<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.student">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShuziActivity" />
        <activity android:name=".QiandaoActivity" />
        <activity android:name=".DengluActivity" />
        <activity android:name=".ZhuceActivity" />
        <activity android:name=".WangjiActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".cuowutimu" />
        <activity android:name=".datifankui"></activity>
        <activity android:name=".APPMainActivity" />
        <activity android:name=".ChatActivity"/>
    </application>

</manifest>